package org.jlab.smoothness.persistence.enumeration;

/** The type of Setting. */
public enum SettingsType {
  /** A String. */
  STRING,
  /** A Comma Separated Values List of Strings. */
  CSV,
  /** A String value of either 'Y' or 'N' representing a boolean Yes (Y) or No (N). */
  BOOLEAN
}
